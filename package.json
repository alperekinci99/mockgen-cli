{
  "name": "mockgen-cli",
  "version": "0.1.1",
  "description": "Generate mock JSON from a live endpoint or a sample file.",
  "type": "module",
  "bin": {
    "mockgen-cli": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/alperekinci99/mockgen-cli.git"
  },
  "bugs": {
    "url": "https://github.com/alperekinci99/mockgen-cli/issues"
  },
  "homepage": "https://github.com/alperekinci99/mockgen-cli#readme",
  "author": "Alper Ekinci",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.18.12",
    "typescript": "^5.6.3"
  }
}
